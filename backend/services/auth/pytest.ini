[pytest]
DJANGO_SETTINGS_MODULE = auth_service.test_settings
pythonpath = .
python_files = tests/**/test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests — no external services
    integration: Integration tests — uses test DB
    e2e: End-to-end tests — requires running Docker services
